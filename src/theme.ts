import { createTheme } from "@mui/material/styles";

export const theme = createTheme({
  palette: {
    primary: {
      // main: "#007bff",
      main: "#ff0000",
    },
    secondary: {
      main: "#ff5733",
    },
    background: {
      default: "#f4f4f4",
    },
    text: {
      primary: "#333",
    },
  },
});
